// Starts the web Server 
// COnfiguring how the API behaves
//sets up the routes (basically our main like in other languages)

import express from "express"; // the framework to help us run the logic and handle the requests

const app = express(); // server object

